"""Fixture apps package for M2.3A inference-wiring tests.

Isolated from ``plugin_apps_fixture`` so adding a backend-declaring app here
does not perturb the plugin-registry tests that assert on that package.
"""
