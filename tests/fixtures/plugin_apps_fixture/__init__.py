"""Fixture apps package for visionplay.core.plugin_registry tests.

Mirrors the real ``apps/`` tree shape (one subpackage per app, an
underscore-prefixed one that must be skipped by discovery) without
depending on real apps existing yet. Not a real app itself — never
discovered on its own.
"""
