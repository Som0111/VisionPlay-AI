"""PySide6 UI shell: main window, launcher, shared widgets, theming.

The only layer (besides ``apps/*/widget.py`` and the app bootstrap) allowed
to import Qt. Everything below (``vision/``, ``core/``) stays headless.
"""
